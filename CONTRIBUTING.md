# Contributing

Keep contributions public-safe and operator-focused.

Before opening a pull request:

```bash
make check
```

Do not add real hosts, public IPs, keypair files, wallet files, provider tokens, private run logs, internal team names, or production-only assumptions.
